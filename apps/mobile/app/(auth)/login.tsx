import { Text, View } from "react-native";
import { Container } from "@/components/Container";

const Login = () => {
	return (
		<Container>
			<View>
				<Text>Login</Text>
			</View>
		</Container>
	);
};

export default Login;
